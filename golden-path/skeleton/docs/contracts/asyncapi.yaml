asyncapi: 3.0.0
info:
  title: <Сервис> events
  version: 0.1.0
# События: <домен>.<сущность>.<действие-в-прошедшем-времени>, версия схемы,
# идемпотентность, гарантии доставки (§5.12). Для T0 схемы — через $ref
# в реестр схем экосистемы.
channels: {}
operations: {}
